{% extends 'base.html.twig' %}


{% block title %}Criar nova serie{% endblock %}

{% block body %}

    {{ form_start(seriesForm, { 'attr': {'novalidate': 'novalidate'} }) }}
    {{ form_row(seriesForm.seriesName) }}
           {{ form_row(seriesForm.seasonsQuantity) }}
           {{ form_row(seriesForm.episodePerSeason) }}
           {{ form_row(seriesForm.coverImage) }}
           {{ form_widget(seriesForm.save, {'attr': {'class': 'btn-dark'}}) }}
    {{ form_end(seriesForm) }}
{% endblock %}
